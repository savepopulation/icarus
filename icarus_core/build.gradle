apply from: "$rootDir/common-android-library.gradle"

dependencies {
    implementation SupportLibraries.appCompat

    // Icarus
    api project(Modules.icarusAnnotations)
    kapt project(Modules.icarusProcessor)
}
